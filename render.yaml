services:
  - type: web
    name: resume-file-host
    runtime: node
    plan: free
    buildCommand: npm ci && npx puppeteer browsers install chrome
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
